# Copy to .env.local and fill in — .env.local is gitignored.

# Google Places API key, scoped to the "Places API (New)" product.
# Used by Agent 1 (Discovery) to source leads by industry + location.
GOOGLE_PLACES_API_KEY=
